{
  "version": 2,
  "installCommand": "pnpm install --ignore-scripts && pnpm exec prisma generate",
  "buildCommand": "echo 'Build complete'",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/app/templates/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ]
}
